import { PopularLiveStream, StreamsStream } from '../../sections'
import './Streams.css'

const Streams =()=>{
    return(
        <>
        <StreamsStream/>
        <PopularLiveStream/>
        </>
    )
}

export default Streams